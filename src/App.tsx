import React from 'react';
import './styles.css';

const App: React.FC = () => {
    return (
        <div className="app">
            <h1>Hello React + TypeScript + Webpack!</h1>
        </div>
    );
};

export default App;